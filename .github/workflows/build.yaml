name: Build Packages

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
    inputs:
      rebuild_all:
        description: 'Rebuild all packages'
        type: boolean
        default: false

jobs:
  build:
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: true
      matrix:
        arch: [x86_64, aarch64, armhf]
        package: [prusalink, otbr, zap-cli]
        include:
          - arch: x86_64
            runner: ubuntu-latest
          - arch: aarch64
            runner: ubuntu-24.04-arm
          - arch: armhf
            runner: ubuntu-24.04-arm
        exclude:
          - arch: armhf
            package: otbr
          - arch: armhf
            package: zap-cli
    steps:
      - uses: actions/checkout@v4

      - uses: chainguard-dev/actions/setup-melange@main
        with:
          version: latest-release

      - name: Setup signing key
        run: |
          echo "${{ secrets.ABUILD_PRIVKEY }}" > melange.pem
          # Convert PKCS8 to PKCS1 format for melange
          openssl rsa -in melange.pem -out melange.rsa -traditional 2>/dev/null || cp melange.pem melange.rsa
          chmod 600 melange.rsa
          rm -f melange.pem

      - name: Setup cache directory
        run: |
          mkdir -p cache
          sudo chmod 777 cache

      - uses: actions/cache@v4
        with:
          path: cache
          key: melange-${{ matrix.package }}-${{ matrix.arch }}-${{ hashFiles(format('{0}.yaml', matrix.package)) }}
          restore-keys: |
            melange-${{ matrix.package }}-${{ matrix.arch }}-

      - name: Build ${{ matrix.package }}
        run: |
          sudo melange build ${{ matrix.package }}.yaml \
            --arch ${{ matrix.arch }} \
            --signing-key melange.rsa \
            --out-dir packages/ \
            --cache-dir cache/ \
            --generate-index=false
          sudo chown -R runner:runner packages/ cache/

      - uses: actions/upload-artifact@v4
        with:
          name: packages-${{ matrix.arch }}-${{ matrix.package }}
          path: packages/${{ matrix.arch }}
          if-no-files-found: ignore

  build-matter-server:
    needs: build
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: true
      matrix:
        arch: [x86_64, aarch64]
        include:
          - arch: x86_64
            runner: ubuntu-latest
          - arch: aarch64
            runner: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v4

      - uses: chainguard-dev/actions/setup-melange@main
        with:
          version: latest-release

      - name: Setup signing key
        run: |
          echo "${{ secrets.ABUILD_PRIVKEY }}" > melange.pem
          openssl rsa -in melange.pem -out melange.rsa -traditional 2>/dev/null || cp melange.pem melange.rsa
          openssl rsa -in melange.rsa -pubout -out melange.rsa.pub
          chmod 600 melange.rsa
          rm -f melange.pem

      - name: Setup cache directory
        run: |
          mkdir -p cache
          sudo chmod 777 cache

      - uses: actions/cache@v4
        with:
          path: cache
          key: melange-matter-server-${{ matrix.arch }}-${{ hashFiles('matter-server.yaml') }}
          restore-keys: |
            melange-matter-server-${{ matrix.arch }}-

      - name: Download zap-cli artifact
        uses: actions/download-artifact@v4
        with:
          name: packages-${{ matrix.arch }}-zap-cli
          path: packages/${{ matrix.arch }}

      - name: Generate local repo index
        run: |
          melange index \
            --signing-key melange.rsa \
            --arch ${{ matrix.arch }} \
            -o packages/${{ matrix.arch }}/APKINDEX.tar.gz \
            packages/${{ matrix.arch }}/*.apk

      - name: Build matter-server
        run: |
          sudo melange build matter-server.yaml \
            --arch ${{ matrix.arch }} \
            --signing-key melange.rsa \
            --keyring-append melange.rsa.pub \
            --repository-append packages/ \
            --out-dir packages/ \
            --cache-dir cache/ \
            --generate-index=false
          sudo chown -R runner:runner packages/ cache/

      - uses: actions/upload-artifact@v4
        with:
          name: packages-${{ matrix.arch }}-matter-server
          path: packages/${{ matrix.arch }}
          if-no-files-found: ignore

  publish:
    needs: [build, build-matter-server]
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
    steps:
      - uses: actions/checkout@v4

      - uses: chainguard-dev/actions/setup-melange@main
        with:
          version: latest-release

      - name: Setup signing key
        run: |
          echo "${{ secrets.ABUILD_PRIVKEY }}" > melange.pem
          # Convert PKCS8 to PKCS1 format for melange
          openssl rsa -in melange.pem -out melange.rsa -traditional 2>/dev/null || cp melange.pem melange.rsa
          chmod 600 melange.rsa
          # Derive public key from private key (SPKI format for APK)
          openssl rsa -in melange.rsa -pubout -out melange.rsa.pub
          rm -f melange.pem

      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          pattern: packages-*
          merge-multiple: false

      - name: Prepare site
        run: |
          mkdir -p _site/keys
          for dir in artifacts/packages-*; do
            # Extract arch from packages-{arch}-{package}
            name=${dir#artifacts/packages-}
            arch=${name%%-*}
            mkdir -p "_site/$arch"
            cp -r "$dir"/* "_site/$arch"/
          done
          # Use the derived public key (matches the signing key)
          cp melange.rsa.pub _site/keys/packages@elohmeier.rsa.pub
          cp index.html _site/

      - name: Generate APK indexes
        run: |
          for arch_dir in _site/*/; do
            arch=$(basename "$arch_dir")
            # Skip non-architecture directories
            case "$arch" in
              x86_64|aarch64|armhf|armv7|x86) ;;
              *) continue ;;
            esac
            echo "Generating APKINDEX for $arch"
            melange index \
              --signing-key melange.rsa \
              --arch "$arch" \
              -o "$arch_dir/APKINDEX.tar.gz" \
              "$arch_dir"/*.apk
          done

      - uses: actions/upload-pages-artifact@v3

      - uses: actions/deploy-pages@v4
