package:
  name: codex
  version: "0.147.0"
  epoch: 1
  description: "OpenAI Codex CLI - coding agent for the terminal"
  copyright:
    - license: Apache-2.0
  target-architecture:
    - x86_64
    - aarch64
  checks:
    disabled:
      - usrmerge

environment:
  contents:
    repositories:
      - https://dl-cdn.alpinelinux.org/alpine/edge/main
    keyring:
      - https://alpinelinux.org/keys/alpine-devel@lists.alpinelinux.org-4a6a0840.rsa.pub
    packages:
      - busybox
      - ca-certificates-bundle
      - curl

pipeline:
  - runs: |
      curl -sSfL -o "codex-${{build.arch}}-unknown-linux-musl.tar.gz" \
        "https://github.com/openai/codex/releases/download/rust-v${{package.version}}/codex-${{build.arch}}-unknown-linux-musl.tar.gz"
      curl -sSfL -o "codex-code-mode-host-${{build.arch}}-unknown-linux-musl.tar.gz" \
        "https://github.com/openai/codex/releases/download/rust-v${{package.version}}/codex-code-mode-host-${{build.arch}}-unknown-linux-musl.tar.gz"
      tar -xzf "codex-${{build.arch}}-unknown-linux-musl.tar.gz"
      tar -xzf "codex-code-mode-host-${{build.arch}}-unknown-linux-musl.tar.gz"
      install -Dm755 "codex-${{build.arch}}-unknown-linux-musl" "${{targets.destdir}}/usr/bin/codex"
      install -Dm755 "codex-code-mode-host-${{build.arch}}-unknown-linux-musl" \
        "${{targets.destdir}}/usr/bin/codex-code-mode-host"

  - runs: |
      mkdir -p "${{targets.destdir}}/usr/share/licenses/codex"
      printf '%s\n' \
        'Copyright 2025 OpenAI' \
        'Licensed under the Apache License, Version 2.0' \
        'https://github.com/openai/codex/blob/main/LICENSE' \
        > "${{targets.destdir}}/usr/share/licenses/codex/LICENSE"

test:
  environment:
    contents:
      repositories:
        - https://dl-cdn.alpinelinux.org/alpine/edge/main
      keyring:
        - https://alpinelinux.org/keys/alpine-devel@lists.alpinelinux.org-4a6a0840.rsa.pub
  pipeline:
    - runs: |
        # Test that binaries exist and are executable
        test -x /usr/bin/codex
        test -x /usr/bin/codex-code-mode-host
    - runs: |
        # Test that codex --version works
        codex --version
    - runs: |
        # Test that codex --help works
        codex --help
